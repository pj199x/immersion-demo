apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 9090
    targetPort: 80
    nodePort: 31003
  selector:
    app: vote
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote-preview
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 9090
    targetPort: 80
    nodePort: 31000
  selector:
    app: vote
